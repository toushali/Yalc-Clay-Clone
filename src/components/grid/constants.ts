/** Grid geometry (mirrors the §5 layout tokens). */
export const ROW_HEIGHT = 36;
export const HEADER_HEIGHT = 40;
export const GUTTER_WIDTH = 48;
export const DEFAULT_COL_WIDTH = 180;
export const MIN_COL_WIDTH = 80;
